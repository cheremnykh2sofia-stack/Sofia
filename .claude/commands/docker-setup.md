---
description: "Create Docker configuration for project"
argument-hint: "[stack]"
---

Create Docker configuration for project.

**Creates:**
- Dockerfile with multi-stage build
- docker-compose.yml
- .dockerignore
- docker-compose.dev.yml

**Example:**
```
/docker-setup fastapi
```

Please setup Docker for: $ARGUMENTS
